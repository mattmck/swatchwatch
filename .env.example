# Local development — used by node-pg-migrate and docker-compose
DATABASE_URL=postgres://postgres:swatchwatch_dev@localhost:5434/swatchwatch

# Auth dev bypass — sends "Bearer dev:1" token from the web app
NEXT_PUBLIC_AUTH_DEV_BYPASS=true
