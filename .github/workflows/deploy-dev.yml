name: Deploy Dev

on:
  push:
    branches: [dev]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy-web:
    name: Deploy Static Web App
    runs-on: ubuntu-latest
    environment: dev
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      
      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          app_location: '/'
          app_build_command: 'npm run build --workspace=packages/shared && npm run build:web'
          # Next.js SSR output (.next) does not include a static index.html.
          # Static Web Apps expects static artifacts, so we deploy Next.js static export output instead.
          output_location: 'apps/web/out'
        env:
          NEXT_PUBLIC_API_URL: 'https://swatchwatch-dev-func-j5jij0be.azurewebsites.net/api'
          NEXT_PUBLIC_AUTH_DEV_BYPASS: ${{ vars.NEXT_PUBLIC_AUTH_DEV_BYPASS }}
          NEXT_PUBLIC_B2C_TENANT: ${{ vars.NEXT_PUBLIC_B2C_TENANT }}
          NEXT_PUBLIC_B2C_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_B2C_CLIENT_ID }}
          NEXT_PUBLIC_B2C_API_SCOPE: ${{ vars.NEXT_PUBLIC_B2C_API_SCOPE }}
          NEXT_PUBLIC_B2C_SIGNUP_SIGNIN_POLICY: ${{ vars.NEXT_PUBLIC_B2C_SIGNUP_SIGNIN_POLICY }}

  deploy-functions:
    name: Deploy Functions
    runs-on: ubuntu-latest
    environment: dev
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build shared package
        run: npm run build --workspace=packages/shared
      
      - name: Build functions
        run: npm run build --workspace=packages/functions
      
      - name: Prepare functions deployment package
        run: |
          DEPLOY_DIR=packages/functions/.deploy
          rm -rf "$DEPLOY_DIR"
          mkdir -p "$DEPLOY_DIR"

          # Copy function app artifacts
          cp packages/functions/host.json "$DEPLOY_DIR/"
          cp packages/functions/package.json "$DEPLOY_DIR/"
          cp -r packages/functions/dist "$DEPLOY_DIR/"

          # Pack the built shared package into a tarball and install from it.
          # Avoid file:directory deps here because npm installs those as symlinks,
          # which can fail to resolve reliably under WEBSITE_RUN_FROM_PACKAGE.
          SHARED_TARBALL="$(npm pack --silent --workspace=packages/shared --pack-destination "$DEPLOY_DIR" | tail -n 1)"

          # Rewrite the workspace reference to the local tarball
          node -e "
            const pkg = JSON.parse(require('fs').readFileSync('$DEPLOY_DIR/package.json', 'utf8'));
            pkg.dependencies['swatchwatch-shared'] = 'file:${SHARED_TARBALL}';
            require('fs').writeFileSync('$DEPLOY_DIR/package.json', JSON.stringify(pkg, null, 2));
          "

          # Install production-only deps into the self-contained deploy dir
          cd "$DEPLOY_DIR"
          npm install --omit=dev

          # Guardrail: ensure the shared package is materialized (not symlinked)
          if [ -L "node_modules/swatchwatch-shared" ]; then
            echo "Expected node_modules/swatchwatch-shared to be a real directory, but it is a symlink."
            ls -la "node_modules/swatchwatch-shared"
            exit 1
          fi

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      
      - name: Run database migrations
        run: |
          if [ -n "$DATABASE_URL" ]; then
            npm run migrate
          else
            echo "⚠ DATABASE_URL not set — skipping migrations"
          fi
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Deploy to Azure Functions
        uses: Azure/functions-action@v1
        with:
          app-name: 'swatchwatch-dev-func-j5jij0be'
          package: 'packages/functions/.deploy'

      - name: Configure function auth settings
        run: |
          az functionapp config appsettings set \
            --name 'swatchwatch-dev-func-j5jij0be' \
            --resource-group 'swatchwatch-dev-rg' \
            --settings \
              AUTH_DEV_BYPASS="$AUTH_DEV_BYPASS" \
              AZURE_AD_B2C_TENANT="$AZURE_AD_B2C_TENANT" \
              AZURE_AD_B2C_CLIENT_ID="$AZURE_AD_B2C_CLIENT_ID"
        env:
          AUTH_DEV_BYPASS: ${{ vars.AUTH_DEV_BYPASS }}
          AZURE_AD_B2C_TENANT: ${{ vars.AZURE_AD_B2C_TENANT || vars.NEXT_PUBLIC_B2C_TENANT }}
          AZURE_AD_B2C_CLIENT_ID: ${{ secrets.AZURE_AD_B2C_CLIENT_ID }}

      - name: Smoke test deployed API
        run: |
          set -euo pipefail
          API_BASE="https://swatchwatch-dev-func-j5jij0be.azurewebsites.net/api"
          ATTEMPTS=8
          SLEEP_SECONDS=10
          AUTH_DEV_BYPASS_VALUE="$(echo "${AUTH_DEV_BYPASS:-false}" | tr '[:upper:]' '[:lower:]')"

          VOICE_URL="$API_BASE/voice"
          echo "Smoke test 1/2: route liveness ($VOICE_URL)"
          for attempt in $(seq 1 "$ATTEMPTS"); do
            echo "Attempt $attempt/$ATTEMPTS..."
            status_code=$(curl -sS -o /tmp/swatchwatch-smoke-voice-body.json -w "%{http_code}" \
              -X POST "$VOICE_URL" \
              -H "Content-Type: application/json" \
              --data '{"probe":"smoke-test"}' || true)

            if [ "$status_code" = "400" ]; then
              echo "Voice smoke test passed (status=$status_code)."
              cat /tmp/swatchwatch-smoke-voice-body.json
              break
            fi

            echo "Unexpected status: $status_code (expected 400)"
            cat /tmp/swatchwatch-smoke-voice-body.json || true

            if [ "$attempt" -eq "$ATTEMPTS" ]; then
              echo "Voice smoke test failed after $ATTEMPTS attempts."
              exit 1
            fi

            sleep "$SLEEP_SECONDS"
          done

          if [ "$AUTH_DEV_BYPASS_VALUE" = "true" ]; then
            POLISHES_URL="$API_BASE/polishes?pageSize=1"
            echo "Smoke test 2/2: authenticated DB path ($POLISHES_URL)"
            for attempt in $(seq 1 "$ATTEMPTS"); do
              echo "Attempt $attempt/$ATTEMPTS..."
              status_code=$(curl -sS -o /tmp/swatchwatch-smoke-polishes-body.json -w "%{http_code}" \
                -H "Authorization: Bearer dev:1" \
                "$POLISHES_URL" || true)

              if [ "$status_code" = "200" ]; then
                echo "Polishes smoke test passed (status=$status_code)."
                cat /tmp/swatchwatch-smoke-polishes-body.json
                exit 0
              fi

              echo "Unexpected status: $status_code (expected 200)"
              cat /tmp/swatchwatch-smoke-polishes-body.json || true

              if [ "$attempt" -eq "$ATTEMPTS" ]; then
                echo "Polishes smoke test failed after $ATTEMPTS attempts."
                exit 1
              fi

              sleep "$SLEEP_SECONDS"
            done
          fi

          echo "Skipping authenticated /polishes smoke check because AUTH_DEV_BYPASS=$AUTH_DEV_BYPASS_VALUE."
          exit 0
        env:
          AUTH_DEV_BYPASS: ${{ vars.AUTH_DEV_BYPASS }}
